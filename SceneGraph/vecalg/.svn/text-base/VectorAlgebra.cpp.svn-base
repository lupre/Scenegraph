#include "VectorAlgebra.h"
#include <iostream>

using namespace std; 

void VectorAlgebra::multVec3d(Vector3d vec, double faktor, Vector3d& result) {
	result.x = vec.x * faktor;
	result.y = vec.y * faktor;
	result.z = vec.z * faktor;
}
